{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pub run webdev build",
    "oldpredeploy": "npm run build && pub run pwa && npm run build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d ./build"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
