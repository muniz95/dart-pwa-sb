{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pub run webdev build",
    "predeploy": "npm run build && pub run pwa && npm run build",
    "deploy": "gh-pages -d ./build"
  }
}